# UI desktop (KivyMD)
# Per il backend web su Lightsail usare requirements-web.txt (senza Kivy).
kivy==2.3.1
kivymd==2.0.0

# Database
mysql-connector-python
pillow

# Segreti / env
python-dotenv

# Backend SaaS (FastAPI)
fastapi
uvicorn[standard]
pydantic[email]
python-multipart
PyJWT
bcrypt

# Stampa e barcode (core.services.printing / core.services.barcode)
reportlab
qrcode
python-escpos
