# Dipendenze del backend SaaS (senza UI desktop Kivy/KivyMD).
# Usare questo file sul server Lightsail / in produzione web.

# Database
mysql-connector-python
pillow

# Segreti / env
python-dotenv

# Backend SaaS (FastAPI)
fastapi
uvicorn[standard]
pydantic[email]
python-multipart
PyJWT
bcrypt

# Stampa e barcode (core.services.printing / core.services.barcode)
reportlab
qrcode
python-escpos
